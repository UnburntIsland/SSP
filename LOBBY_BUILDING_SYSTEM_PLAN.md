# 可走動大廳與建築系統規劃

## 1. 目標

把目前以選單為主的首頁升級為可操作的 2D 大廳：

- 玩家控制目前選定角色在大廳移動。
- 走到傳送門並互動後，開啟既有關卡選擇，確認後進入戰鬥。
- 玩家可進入掛機回收區累積「再生材料」，每日取得量有上限。
- 再生材料可用來建造功能建築與裝飾。
- 建築採固定模組，但可在合法區域內自由擺放、旋轉及收納。
- 功能建築每一種類只能建造一棟，效果不可重複堆疊。
- 裝飾可以重複建造，數量由大廳可用空間自然限制。
- 功能建築的效果只修改進入關卡後的玩家能力，不影響敵人、掉落與關卡資料。

## 2. 與現有專案的整合原則

目前專案已有：

- `screen-menu` 首頁與關卡選擇。
- 戰鬥用 `game-canvas`。
- 5 個角色及 8 方向角色動畫 resolver。
- 3 個依序解鎖的關卡。
- `senloop_save_v1` localStorage 存檔。
- 循環商店永久能力、角色被動與角色成長點數。
- 大廳背景圖 `assets/images/backgrounds/lobby_background.png`。

建議不要把 `Game` 戰鬥物件改造成大廳。新增獨立 `LobbyScene`，共用 Canvas、Input、Animation、Assets 與目前選定角色資料即可。這可避免敵人生成、戰鬥特效、勝敗流程與大廳狀態互相污染。

建議 App 狀態：

```text
LOBBY
LOBBY_BUILD
LOBBY_MODAL
PORTAL_SELECT
PLAYING
PAUSED
VICTORY
GAME_OVER
```

回到大廳時必須停止戰鬥 loop、清空上一局世界物件，再啟動 `LobbyScene`，因此不會殘留上一場遊戲畫面。

## 3. 大廳空間配置

### 基本尺寸

- 世界尺寸建議：`1600 x 1000` world pixels。
- 建造格線：`32 x 32` world pixels。
- 鏡頭跟隨玩家，並限制在地圖邊界。
- 建築圖可超出 footprint，但碰撞與擺放判定只使用 footprint。

### 固定區域

1. **中央廣場**
   - 玩家登入與回城出生點。
   - 保留至少 `5 x 5` 格不可建造安全區。

2. **行動傳送門**
   - 固定於大廳上方。
   - 玩家接近時顯示互動提示。
   - 互動後開啟既有關卡卡片，不直接開始戰鬥。
   - 已鎖定關卡沿用目前「擊敗前一關 BOSS」規則。

3. **掛機回收區**
   - 固定於大廳右側或右下。
   - 玩家站入區域 2 秒後開始累積。
   - 移出區域立即停止。
   - 顯示今日進度與距離下一份材料的秒數。

4. **建造工作台**
   - 固定於大廳左側或左下。
   - 互動後進入建造模式。
   - 同時提供建築圖鑑、效果及材料需求。

5. **保留通道**
   - 出生點到傳送門、掛機區、工作台之間保留至少 3 格寬通道。
   - 放置建築後進行可達性檢查，禁止玩家把必要設施完全堵住。

## 4. 傳送門流程

```text
玩家靠近傳送門
→ 顯示「互動：選擇行動地點」
→ 玩家按 E / 手機點擊互動鍵
→ 遊戲暫停大廳移動
→ 顯示現有地圖輪播卡
→ 確認「開始遊戲」
→ 保存大廳角色位置與配置
→ 彙整建築加成
→ 建立 Player 並進入關卡
```

取消地圖選擇後返回大廳原位置。傳送門碰撞本身不啟動關卡，避免玩家誤觸。

## 5. 再生材料與掛機區

### 單一材料

第一版只使用一種新貨幣：

```text
名稱：再生材料
用途：建築與裝飾
與循環幣分開
```

先維持單一材料，避免小學生同時管理過多資源。未來如需擴充，可再拆成木材、金屬與再生塑料。

### 建議產出

- 掛機區每 20 秒取得 1 份再生材料。
- 每日掛機上限 60 份，約 20 分鐘達到上限。
- 進度顯示為 `今日回收 24 / 60`。
- 達上限時停止計時，顯示「今日回收已完成」。
- 每日依玩家本地時區的日期重置。

### 計時規則

- 只有角色已進入掛機區才計時。
- 使用 timestamp 差值，不依賴每幀更新，避免瀏覽器分頁節流造成漏算。
- 單次可結算時間差應設上限，例如 5 分鐘，避免系統時間被大幅修改後一次灌入大量材料。
- 結算仍受每日 60 份上限限制。
- 第一版不做完全離線收益；玩家關閉頁面後停止，避免「掛機」變成不透明的離線獎勵。

### 非掛機補充來源

為避免鼓勵玩家只站著不玩，可讓 BOSS 首次或每日首勝提供少量再生材料：

- 第 1 關：8
- 第 2 關：12
- 第 3 關：18

此獎勵與掛機每日上限分開，但每個關卡每日只領一次。

## 6. 建築分類

### 功能建築

每一種 `buildingId` 最多一棟。即使收納後仍視為已擁有，但只有擺放在大廳內的建築生效。

| ID | 名稱 | Footprint | 建議成本 | 戰鬥效果 |
|---|---|---:|---:|---|
| `solar_workshop` | 太陽能工坊 | 4 x 4 | 80 | 攻擊力 +5% |
| `rain_garden` | 雨水花園 | 4 x 3 | 70 | 最大生命 +8% |
| `wind_station` | 微型風力站 | 3 x 3 | 65 | 移動速度 +4% |
| `recycle_guard` | 循環防護站 | 4 x 4 | 100 | 每 90 秒格擋一次傷害 |

平衡原則：

- 加成比循環商店與角色被動低，作為長期輔助而非取代原系統。
- 功能建築效果不因建造多個裝飾而增加。
- 同一功能建築不能重複建造。
- 格擋只取消一次 `takeDamage`，不延長無敵時間、不修改敵人傷害。
- 建築效果在進入關卡時建立快照，本局途中移動建築不會改變進行中的戰鬥。

### 裝飾建築

第一批建議 8 種：

- 小樹
- 花圃
- 回收長椅
- 太陽能路燈
- 分類垃圾桶
- 雨水桶
- 木製告示牌
- 小型生態池

規則：

- 同一裝飾可重複建造。
- 不提供戰鬥效果。
- 可碰撞裝飾與純視覺裝飾需在資料中明確區分。
- 可隨時收納並重新擺放；收納不返還材料，物件回到玩家庫存。
- 暫不提供永久拆除，避免誤操作造成損失。

## 7. 建造與擺放操作

### 進入建造模式

- 靠近工作台後互動，或使用大廳固定建造圖示。
- 建造模式中角色停止移動。
- 世界維持可平移與縮放，但不進行掛機計時。

### 操作流程

```text
選擇建築
→ 顯示半透明 ghost
→ 拖曳或方向鍵移動
→ 自動吸附 32px 格線
→ 綠色代表合法、紅色代表不可放置
→ 可旋轉 90 度
→ 確認放置或取消
```

桌面：

- 滑鼠拖曳。
- `R` 旋轉。
- 左鍵確認。
- `Esc` 取消。

手機與平板：

- 單指拖曳建築。
- 雙指平移或縮放大廳。
- 底部固定旋轉、確認、取消圖示。
- 所有觸控按鈕至少 48px 高。

### 放置驗證

建築必須同時符合：

1. footprint 完全位於可建造區。
2. 不與其他建築 footprint 重疊。
3. 不與傳送門、掛機區、工作台、出生區重疊。
4. 不穿過大廳固定牆面或水域。
5. 放置後出生點仍可走到傳送門、掛機區及工作台。
6. 功能建築尚未存在同 ID 的另一棟。
7. 玩家擁有足夠材料或庫存中已有該物件。

第 5 項建議使用格線 flood-fill 檢查，不只檢查 AABB，避免玩家用建築排成一整面牆。

### 編輯已放置建築

點選建築後顯示：

- 建築名稱。
- 效果或「純裝飾」。
- 移動。
- 旋轉。
- 收納。

點擊其他地方關閉面板，延續目前技能資訊面板的操作習慣。

## 8. 建築效果彙整

不要讓 `Player` 自己掃描大廳物件。由單一函式先彙整：

```js
GameData.getLobbyBonuses(lobbySave)
```

建議輸出：

```js
{
  damageMult: 1.05,
  maxHpMult: 1.08,
  speedMult: 1.04,
  blockInterval: 90,
  sources: [
    { buildingId: "solar_workshop", label: "攻擊力 +5%" }
  ]
}
```

進入關卡時合併順序：

```text
基礎數值
→ 循環商店永久升級
→ 角色被動
→ 角色成長點數
→ 大廳建築加成
→ 本局升級
```

百分比倍率各自只套用一次。結果畫面的數值來源可列出「大廳建築」，方便 QA 與玩家理解。

## 9. 存檔設計

建議將存檔 schema 升級為 4，新增：

```js
lobby: {
  version: 1,
  playerPosition: { x: 800, y: 620, direction: "N" },
  materials: {
    recycled: 0
  },
  daily: {
    dateKey: "2026-07-25",
    idleEarned: 0,
    dailyBossClaims: {}
  },
  inventory: {
    small_tree: 0
  },
  buildings: [
    {
      instanceId: "building-1",
      buildingId: "solar_workshop",
      x: 384,
      y: 256,
      rotation: 0,
      level: 1,
      placed: true
    }
  ]
}
```

存檔遷移要求：

- 舊存檔沒有 `lobby` 時自動補預設值。
- 不覆蓋既有角色、循環幣、商店、圖鑑與關卡進度。
- 載入時移除未知 building ID 前，先將它放到 `orphanedBuildings`，不要直接刪除。
- 所有材料扣除與建築新增在同一次 save transaction 完成，避免刷新造成材料已扣但建築不存在。

## 10. 建築資料結構

新增資料檔建議：

```text
js/data/lobbyBuildings.js
```

資料範例：

```js
{
  id: "solar_workshop",
  name: "太陽能工坊",
  category: "functional",
  unique: true,
  cost: { recycled: 80 },
  footprint: { w: 4, h: 4 },
  collision: true,
  rotations: [0, 90, 180, 270],
  assetBasePath: "assets/images/lobby/buildings/solar_workshop/",
  effect: { stat: "damageMult", op: "multiply", value: 1.05 },
  knowledgeId: "building_solar_workshop",
  description: "把回收材料轉為穩定能源，出擊時攻擊力提升 5%。"
}
```

所有碰撞、成本、效果與圖片路徑均資料驅動，建造程式不使用大量 `if buildingId`。

## 11. UI/UX

### 大廳常駐 HUD

左上：

- 再生材料數量。
- 今日掛機取得量 `0 / 60`。

右上：

- 角色。
- 建造。
- 商店/扭蛋。
- 圖鑑/成就。
- 設定。

畫面下方：

- 靠近互動物時才出現互動提示。
- 手機顯示互動鍵；桌面顯示 `E`。

### 掛機區提示

```text
資源回收中
下一份材料：12 秒
今日：24 / 60
```

達上限：

```text
今日回收已完成
明日可再次取得 60 份
```

### 建築圖鑑

每張建築項目顯示：

- 建築圖片。
- 名稱。
- 功能/裝飾標記。
- 效果。
- footprint。
- 已擁有數量。
- 材料需求。
- 可建造/材料不足/已建造狀態。

功能建築已存在時顯示「已建造」，不再顯示購買按鈕。

## 12. 大廳角色與碰撞

- 使用目前選定角色的 `idle/walk` 8 方向動畫。
- 沿用現有 input mapping 與 `getDirectionFromVector()`，不建立第二套方向公式。
- 大廳移動速度可固定為角色基礎速度，不套用戰鬥加速技能。
- 玩家以腳底附近小圓形 collider 移動，避免角色圖片透明留白影響碰撞。
- 建築採 footprint AABB；碰撞時分 X/Y 軸處理，避免卡角。
- 大廳沒有攻擊、敵人、傷害、技能與經驗系統。

## 13. 素材管線

建議資料夾：

```text
assets/images/lobby/
  ground/
  portal/
  stations/
  buildings/
    solar_workshop/
    rain_garden/
    wind_station/
    recycle_guard/
  decorations/
```

新素材沿用現有管線：

```text
_incoming → QA → normalized → runtime
```

素材 QA：

- 透明背景。
- 不含 UI 或文字。
- 同一透視角度。
- 統一像素密度。
- anchor 與 footprint 對齊。
- 四個旋轉方向尺寸一致。
- 不遮住玩家互動提示。
- 72px 角色放在旁邊時比例合理。

第一版所需素材：

- 1 張可平鋪大廳地面或一套 tile。
- 傳送門 4 至 6 幀待機動畫。
- 掛機回收區 4 幀運作動畫。
- 建造工作台 1 張。
- 4 棟功能建築，每棟至少正面 1 張。
- 8 種裝飾。
- 建造目錄 icon。
- 再生材料 icon。

為降低第一版成本，建築先不支援四向美術旋轉；旋轉只改 footprint。待功能驗證後，再補 0/90/180/270 度圖片。

## 14. 程式模組建議

新增：

```text
js/data/lobbyBuildings.js
js/lobby.js
js/lobbyPlacement.js
js/lobbyEconomy.js
```

修改範圍：

- `index.html`：載入新模組、加入大廳 HUD 與建造面板。
- `js/main.js`：加入 LOBBY 狀態與傳送門轉場。
- `js/storage.js`：schema 4、材料、每日進度、建築配置。
- `js/assets.js`：大廳與建築 manifest。
- `js/ui.js`：建築目錄、互動提示、材料 HUD。
- `js/player.js`：只接收彙整後的 building bonus；不讀大廳配置。
- `css/style.css`：大廳 HUD、建造模式、手機版 bottom sheet。

不要修改：

- `js/input.js` 的方向 mapping。
- `getDirectionFromVector()`。
- 敵人 AI。
- 關卡方向 resolver。
- 既有技能傷害、冷卻與碰撞。

## 15. 失敗與邊界情況

- **材料不足**：不進入放置確認，顯示還差多少。
- **放置失敗**：ghost 保持紅色，不扣材料。
- **刷新頁面**：已確認的建築保留；未確認 ghost 不保存。
- **功能建築重複**：目錄顯示已建造，不能再次購買。
- **建築堵路**：可達性檢查失敗，不准放置。
- **素材 404**：顯示乾淨 Canvas placeholder，配置仍可正常載入。
- **日期改動**：每日產出始終 clamp 到 60，單次補算不超過 5 分鐘。
- **存檔空間過大**：只保存建築資料，不保存渲染快取或圖片。
- **角色切換**：大廳角色立即更換；建築效果屬帳號，不綁角色。
- **關卡重試**：重新建立加成快照，不能在同一 Player 上重複乘算。

## 16. 驗收標準

### 大廳

- 桌面、手機、平板都可移動。
- 8 方向顯示與目前角色動畫一致。
- 不會走出地圖或穿過碰撞建築。
- 回城後不殘留戰鬥畫面。

### 傳送門

- 接近才顯示提示。
- 不會因碰到傳送門立即開戰。
- 鎖定關卡不可進入。
- 取消後回到原大廳位置。

### 掛機收益

- 每 20 秒正確增加 1。
- 移出區域停止。
- 每日不超過 60。
- 重新整理不重複領取。
- 日期切換後正確重置。

### 建造

- 不能重疊。
- 不能蓋在固定設施上。
- 不能堵死必要通路。
- 功能建築每種只能一棟。
- 裝飾可以重複建造。
- 收納與重新放置不遺失物件。

### 戰鬥加成

- 攻擊、生命、速度只增加一次。
- 沒有功能建築時數值與目前版本完全一致。
- 拆下或收納建築後，下一局不再生效。
- 格擋只取消一次傷害並正確重新計時。
- 敵人、關卡、技能資料未被修改。

## 17. 分階段實作

### Phase 1：可走動大廳

- 新增 `LobbyScene`。
- 使用目前角色 8 方向動畫。
- 大廳鏡頭與碰撞。
- 傳送門互動。
- 既有關卡選擇由傳送門開啟。
- 勝利、死亡、返回都正確回到大廳。

### Phase 2：掛機材料

- 新增再生材料與存檔遷移。
- 掛機回收區。
- 每日 60 上限與進度 HUD。
- timestamp 結算與日期重置。

### Phase 3：建造與裝飾

- 建築資料表。
- 建造目錄與 ghost。
- 格線、旋轉、碰撞、保留區。
- 收納與重新放置。
- 先上 8 種無效果裝飾，驗證自由配置。

### Phase 4：功能建築

- 4 棟唯一功能建築。
- 大廳加成彙整。
- 戰鬥開局快照。
- 格擋效果。
- 數值來源顯示與平衡 QA。

### Phase 5：美術與教育內容

- GPT-image 建築與裝飾素材。
- 傳送門、掛機區動畫。
- 每棟建築的永續知識卡。
- 手機與平板視覺 QA。
- 效能、存檔與回歸測試。

## 18. 建議第一版範圍

第一版先完成：

- 1 張大廳地圖。
- 1 個傳送門。
- 1 個掛機回收區。
- 1 種再生材料。
- 每日上限 60。
- 4 種功能建築。
- 8 種可重複裝飾。
- 自由擺放、旋轉、收納、防重疊與防堵路。

第一版暫不做：

- 多人大廳。
- 拜訪其他玩家。
- 建築升級樹。
- 多種建材。
- 完全離線收益。
- 建築生產鏈。
- 建築拆除與材料退款。

這個範圍足以先驗證三件最重要的事：大廳是否好走、建造是否好用、永久加成是否有吸引力且不破壞平衡。
